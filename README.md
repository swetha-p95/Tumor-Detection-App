This app contains a very basic brain tumour identification model that has been trained on a small dataset containing 500 images of the brain with and without tumour. The model uses Convolutional Neural Netwroks (CNNs) for tumour detection. 

